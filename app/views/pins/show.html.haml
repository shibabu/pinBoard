%h1
  = @pin.title
  = link_to 'Edit', edit_pin_path(@pin), class: 'btn btn-success'
%p= @pin.description
= link_to 'Back', root_path, class: 'btn btn-default'
= link_to 'Delete', @pin, class: 'btn btn-danger pull-right', data: {confirm: 'You are about to delete the Pin!'}, method: :delete
